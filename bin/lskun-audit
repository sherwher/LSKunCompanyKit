#!/usr/bin/env python3
"""CPO 결재 기록 진입점 (ADR-0027). 본체는 src/lskun_kit/cli_audit.py."""

import sys
from pathlib import Path

sys.path.insert(0, str(Path(__file__).resolve().parent.parent / "src"))

from lskun_kit.cli_audit import main  # noqa: E402

if __name__ == "__main__":
    sys.exit(main())
